// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/award"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/nominee"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/predicate"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/school"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/voteitem"
	"github.com/inscripoem/bta-voting-system/backend/internal/ent/votingsession"
)

// AwardQuery is the builder for querying Award entities.
type AwardQuery struct {
	config
	ctx           *QueryContext
	order         []award.OrderOption
	inters        []Interceptor
	predicates    []predicate.Award
	withSession   *VotingSessionQuery
	withSchool    *SchoolQuery
	withNominees  *NomineeQuery
	withVoteItems *VoteItemQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AwardQuery builder.
func (_q *AwardQuery) Where(ps ...predicate.Award) *AwardQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AwardQuery) Limit(limit int) *AwardQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AwardQuery) Offset(offset int) *AwardQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AwardQuery) Unique(unique bool) *AwardQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AwardQuery) Order(o ...award.OrderOption) *AwardQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySession chains the current query on the "session" edge.
func (_q *AwardQuery) QuerySession() *VotingSessionQuery {
	query := (&VotingSessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(award.Table, award.FieldID, selector),
			sqlgraph.To(votingsession.Table, votingsession.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, award.SessionTable, award.SessionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySchool chains the current query on the "school" edge.
func (_q *AwardQuery) QuerySchool() *SchoolQuery {
	query := (&SchoolClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(award.Table, award.FieldID, selector),
			sqlgraph.To(school.Table, school.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, award.SchoolTable, award.SchoolColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNominees chains the current query on the "nominees" edge.
func (_q *AwardQuery) QueryNominees() *NomineeQuery {
	query := (&NomineeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(award.Table, award.FieldID, selector),
			sqlgraph.To(nominee.Table, nominee.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, award.NomineesTable, award.NomineesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVoteItems chains the current query on the "vote_items" edge.
func (_q *AwardQuery) QueryVoteItems() *VoteItemQuery {
	query := (&VoteItemClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(award.Table, award.FieldID, selector),
			sqlgraph.To(voteitem.Table, voteitem.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, award.VoteItemsTable, award.VoteItemsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Award entity from the query.
// Returns a *NotFoundError when no Award was found.
func (_q *AwardQuery) First(ctx context.Context) (*Award, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{award.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AwardQuery) FirstX(ctx context.Context) *Award {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Award ID from the query.
// Returns a *NotFoundError when no Award ID was found.
func (_q *AwardQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{award.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AwardQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Award entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Award entity is found.
// Returns a *NotFoundError when no Award entities are found.
func (_q *AwardQuery) Only(ctx context.Context) (*Award, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{award.Label}
	default:
		return nil, &NotSingularError{award.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AwardQuery) OnlyX(ctx context.Context) *Award {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Award ID in the query.
// Returns a *NotSingularError when more than one Award ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AwardQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{award.Label}
	default:
		err = &NotSingularError{award.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AwardQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Awards.
func (_q *AwardQuery) All(ctx context.Context) ([]*Award, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Award, *AwardQuery]()
	return withInterceptors[[]*Award](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AwardQuery) AllX(ctx context.Context) []*Award {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Award IDs.
func (_q *AwardQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(award.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AwardQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AwardQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AwardQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AwardQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AwardQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AwardQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AwardQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AwardQuery) Clone() *AwardQuery {
	if _q == nil {
		return nil
	}
	return &AwardQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]award.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.Award{}, _q.predicates...),
		withSession:   _q.withSession.Clone(),
		withSchool:    _q.withSchool.Clone(),
		withNominees:  _q.withNominees.Clone(),
		withVoteItems: _q.withVoteItems.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSession tells the query-builder to eager-load the nodes that are connected to
// the "session" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AwardQuery) WithSession(opts ...func(*VotingSessionQuery)) *AwardQuery {
	query := (&VotingSessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSession = query
	return _q
}

// WithSchool tells the query-builder to eager-load the nodes that are connected to
// the "school" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AwardQuery) WithSchool(opts ...func(*SchoolQuery)) *AwardQuery {
	query := (&SchoolClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSchool = query
	return _q
}

// WithNominees tells the query-builder to eager-load the nodes that are connected to
// the "nominees" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AwardQuery) WithNominees(opts ...func(*NomineeQuery)) *AwardQuery {
	query := (&NomineeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNominees = query
	return _q
}

// WithVoteItems tells the query-builder to eager-load the nodes that are connected to
// the "vote_items" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AwardQuery) WithVoteItems(opts ...func(*VoteItemQuery)) *AwardQuery {
	query := (&VoteItemClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVoteItems = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Award.Query().
//		GroupBy(award.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AwardQuery) GroupBy(field string, fields ...string) *AwardGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AwardGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = award.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Award.Query().
//		Select(award.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *AwardQuery) Select(fields ...string) *AwardSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AwardSelect{AwardQuery: _q}
	sbuild.label = award.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AwardSelect configured with the given aggregations.
func (_q *AwardQuery) Aggregate(fns ...AggregateFunc) *AwardSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AwardQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !award.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AwardQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Award, error) {
	var (
		nodes       = []*Award{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withSession != nil,
			_q.withSchool != nil,
			_q.withNominees != nil,
			_q.withVoteItems != nil,
		}
	)
	if _q.withSession != nil || _q.withSchool != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, award.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Award).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Award{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSession; query != nil {
		if err := _q.loadSession(ctx, query, nodes, nil,
			func(n *Award, e *VotingSession) { n.Edges.Session = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSchool; query != nil {
		if err := _q.loadSchool(ctx, query, nodes, nil,
			func(n *Award, e *School) { n.Edges.School = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNominees; query != nil {
		if err := _q.loadNominees(ctx, query, nodes,
			func(n *Award) { n.Edges.Nominees = []*Nominee{} },
			func(n *Award, e *Nominee) { n.Edges.Nominees = append(n.Edges.Nominees, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVoteItems; query != nil {
		if err := _q.loadVoteItems(ctx, query, nodes,
			func(n *Award) { n.Edges.VoteItems = []*VoteItem{} },
			func(n *Award, e *VoteItem) { n.Edges.VoteItems = append(n.Edges.VoteItems, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AwardQuery) loadSession(ctx context.Context, query *VotingSessionQuery, nodes []*Award, init func(*Award), assign func(*Award, *VotingSession)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Award)
	for i := range nodes {
		if nodes[i].voting_session_awards == nil {
			continue
		}
		fk := *nodes[i].voting_session_awards
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(votingsession.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "voting_session_awards" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AwardQuery) loadSchool(ctx context.Context, query *SchoolQuery, nodes []*Award, init func(*Award), assign func(*Award, *School)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Award)
	for i := range nodes {
		if nodes[i].school_awards == nil {
			continue
		}
		fk := *nodes[i].school_awards
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(school.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "school_awards" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AwardQuery) loadNominees(ctx context.Context, query *NomineeQuery, nodes []*Award, init func(*Award), assign func(*Award, *Nominee)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Award)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Nominee(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(award.NomineesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.award_nominees
		if fk == nil {
			return fmt.Errorf(`foreign-key "award_nominees" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "award_nominees" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AwardQuery) loadVoteItems(ctx context.Context, query *VoteItemQuery, nodes []*Award, init func(*Award), assign func(*Award, *VoteItem)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Award)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.VoteItem(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(award.VoteItemsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.award_vote_items
		if fk == nil {
			return fmt.Errorf(`foreign-key "award_vote_items" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "award_vote_items" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *AwardQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AwardQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(award.Table, award.Columns, sqlgraph.NewFieldSpec(award.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, award.FieldID)
		for i := range fields {
			if fields[i] != award.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AwardQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(award.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = award.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AwardGroupBy is the group-by builder for Award entities.
type AwardGroupBy struct {
	selector
	build *AwardQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AwardGroupBy) Aggregate(fns ...AggregateFunc) *AwardGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AwardGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AwardQuery, *AwardGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AwardGroupBy) sqlScan(ctx context.Context, root *AwardQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AwardSelect is the builder for selecting fields of Award entities.
type AwardSelect struct {
	*AwardQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AwardSelect) Aggregate(fns ...AggregateFunc) *AwardSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AwardSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AwardQuery, *AwardSelect](ctx, _s.AwardQuery, _s, _s.inters, v)
}

func (_s *AwardSelect) sqlScan(ctx context.Context, root *AwardQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
